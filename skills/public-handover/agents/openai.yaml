interface:
  display_name: "Public Handover"
  short_description: "Publish shared summary from generator and evaluator"
  default_prompt: "Check audit preconditions and if passed publish a numbered session handover, historical copy, and update handover/public.md index."
