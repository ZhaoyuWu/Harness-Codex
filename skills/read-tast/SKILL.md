---
name: read-tast
description: Compatibility alias for read-task. Use when /read-tast is triggered by typo and route to the same task-reading behavior.
---

# Read Tast

This is a compatibility alias of `$read-task`.

## Workflow

1. Follow the exact same behavior as `$read-task`.
2. Mention that this command is an alias for `$read-task`.
